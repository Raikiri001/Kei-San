/** Shared small numeric-input styling used across radial menu panels (color wheel, size fields). */
export const numberInputClass =
  "glass-panel h-7 w-12 shrink-0 rounded-full px-1 text-center text-[11px] tabular-nums outline-none focus:border-accent/60";

/** Small uppercase caption used above/beside inputs — matches ColorSwatchPanel's group label convention. */
export const fieldLabelClass = "text-[9px] uppercase tracking-wide opacity-60";
